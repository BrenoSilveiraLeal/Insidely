import { CompanyCard } from "@/components/cards";
import { PublicShell } from "@/components/public-shell";
import { getPublicCompaniesPage } from "@/lib/queries";
export const dynamic = "force-dynamic";
export default async function CompaniesPage({ searchParams }: { searchParams: Promise<{ page?: string }> }) { const page = Math.max(1, Number((await searchParams).page) || 1); const result = await getPublicCompaniesPage(page); return <PublicShell><section className="page-hero" data-mark="E"><div className="container page-hero-inner"><span className="eyebrow">Empresas</span><h1>Uma marca. Muitas realidades.</h1><p>Encontre profissionais com experiência contextualizada em diferentes áreas, momentos e equipes.</p></div></section><section className="section"><div className="container"><div className="grid grid-4">{result.items.map(company=><CompanyCard key={company.id} company={company}/>)}</div><div className="pagination"><span>{result.total} empresas · página {result.page}</span>{page > 1 && <a className="button button-ghost" href={`/empresas?page=${page - 1}`}>Anterior</a>}{page * result.pageSize < result.total && <a className="button button-dark" href={`/empresas?page=${page + 1}`}>Próxima</a>}</div></div></section></PublicShell>; }
