import { Role } from "@prisma/client";import { DashboardShell } from "@/components/dashboard-shell";import { requireUser } from "@/lib/session";
export default async function Page(){const u=await requireUser([Role.USER,Role.ADMIN]);return <DashboardShell mode="user" title="Configurações"><div className="panel form-stack"><div className="field"><label>Nome</label><input className="input" value={u.name??""} readOnly/></div><div className="field"><label>E-mail</label><input className="input" value={u.email??""} readOnly/></div><p className="muted">Edição de credenciais foi mantida fora do escopo acadêmico. Sessão, identidade e autorização são reais.</p></div></DashboardShell>}
