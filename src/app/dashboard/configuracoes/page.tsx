import { getUserById } from "@/lib/queries";
import { Role } from "@/lib/domain";import { DashboardShell } from "@/components/dashboard-shell";import { AccountDangerZone, ProfileImageForm } from "@/components/profile-management";import { TwoFactorEnabled, TwoFactorSetup } from "@/components/two-factor";import { getOrCreateTwoFactorSetup } from "@/app/actions";import { requireUser } from "@/lib/session";
export default async function Page(){const u=await requireUser([Role.USER,Role.ADMIN]);const stored=await getUserById(u.id);if(!stored)return null;const setup=!stored.twoFactorEnabled?await getOrCreateTwoFactorSetup():null;return <DashboardShell mode="user" title="Minha conta"><div className="profile-management-stack"><ProfileImageForm image={stored.image} name={stored.name}/><div className="panel form-stack"><span className="eyebrow">Conta de quem explora</span><h2>Seus dados para entrar, agendar e acompanhar conversas.</h2><div className="field"><label>Nome</label><input className="input" value={stored.name} readOnly/></div><div className="field"><label>E-mail</label><input className="input" value={stored.email} readOnly/></div><p className="muted">Esta conta nÃ£o Ã© um perfil pÃºblico de consultor. Perfis profissionais possuem experiÃªncia, verificaÃ§Ã£o, valor e regras prÃ³prias.</p></div>{stored.twoFactorEnabled?<TwoFactorEnabled/>:setup?<TwoFactorSetup uri={setup.uri}/>:null}<AccountDangerZone email={stored.email}/></div></DashboardShell>}
