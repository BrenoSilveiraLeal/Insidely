import { LifeBuoy, MessageCircleMore, ShieldCheck } from "lucide-react";
import { submitSupportAction } from "@/app/actions";
import { PublicShell } from "@/components/public-shell";

export default async function SupportPage({searchParams}:{searchParams:Promise<{enviado?:string;erro?:string}>}){const params=await searchParams;return <PublicShell><section className="page-hero" data-mark="?"><div className="container page-hero-inner"><span className="eyebrow"><LifeBuoy size={15}/> Suporte Insidely</span><h1>Conte com a gente.</h1><p>D sobre conta, verifica, pagamento, conversa ou seguran podem ser enviadas diretamente para a equipe.</p></div></section><section className="section"><div className="container support-grid"><div><span className="eyebrow">Atendimento respons?vel</span><h2 className="section-title">Explique o que aconteceu.</h2><p className="section-copy">N envie senhas, n completos de cart?o ou documentos nesta mensagem. Para verifica profissional, use a  privada do perfil do consultor.</p><div className="support-points"><span><MessageCircleMore/> Mensagem registrada no seu perfil</span><span><ShieldCheck/> An pela equipe de suporte</span></div></div><form className="panel form-stack" action={submitSupportAction}>{params.enviado&&<p className="success-box">Mensagem enviada. A equipe poder? acompanhar sua solicita.</p>}{params.erro&&<p className="form-error">Preencha a mensagem com pelo menos 20 caracteres.</p>}<div className="field"><label htmlFor="category">Assunto</label><select className="select" id="category" name="category" required><option value="Conta e acesso">Conta e acesso</option><option value="Verifica profissional">Verifica profissional</option><option value="Agendamento">Agendamento</option><option value="Pagamento">Pagamento</option><option value="Seguran?a e den?ncia">Seguran?a e den?ncia</option><option value="Outro assunto">Outro assunto</option></select></div><div className="field"><label htmlFor="description">Como podemos ajudar?</label><textarea className="textarea" id="description" name="description" minLength={20} maxLength={2000} required/></div><button className="button button-accent" type="submit">Enviar para o suporte</button></form></div></section></PublicShell>}
