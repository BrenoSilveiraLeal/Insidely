import { Role } from "@prisma/client";import { DashboardShell } from "@/components/dashboard-shell";import { prisma } from "@/lib/prisma";import { requireUser } from "@/lib/session";
export const dynamic="force-dynamic";export default async function Page(){await requireUser([Role.ADMIN]);const users=await prisma.user.findMany({orderBy:{createdAt:"desc"},take:100,include:{professionalProfile:true}});return <DashboardShell mode="admin" title="Usuários"><div className="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Papel</th><th>Perfil</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.professionalProfile?.verificationStatus??"—"}</td></tr>)}</tbody></table></div></DashboardShell>}
