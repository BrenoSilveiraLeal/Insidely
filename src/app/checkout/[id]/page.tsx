import { notFound } from "next/navigation";
import { payBookingAction } from "@/app/actions";
import { PublicShell } from "@/components/public-shell";
import { money, shortDate } from "@/lib/format";
import { prisma } from "@/lib/prisma";
import { requireUser } from "@/lib/session";
export const dynamic="force-dynamic";
export default async function CheckoutPage({params}:{params:Promise<{id:string}>}){const user=await requireUser();const {id}=await params;const booking=await prisma.booking.findFirst({where:{id,customerId:user.id},include:{professional:{include:{user:true}},payment:true}});if(!booking)notFound();const professionalShare=booking.subtotalCents-booking.feeCents;const platformPercent=Math.round((booking.feeCents/booking.subtotalCents)*100);return <PublicShell><section className="page-hero" data-mark="R$"><div className="container page-hero-inner"><span className="eyebrow">Pagamento da conversa</span><h1>Confirme o encontro.</h1><p>Escolha Pix ou cartão e confira a divisão antes de continuar.</p></div></section><section className="section"><div className="container profile-layout"><div className="panel"><span className="eyebrow">Resumo transparente</span><ul className="detail-list"><li><span>Profissional</span><strong>{booking.professional.user.name}</strong></li><li><span>Data</span><strong>{shortDate(booking.startsAt)}</strong></li><li><span>Duração</span><strong>{booking.durationMinutes} min</strong></li><li><span>Total da conversa</span><strong>{money(booking.totalCents)}</strong></li><li><span>Repasse ao profissional · {100-platformPercent}%</span><strong>{money(professionalShare)}</strong></li><li><span>Insidely · {platformPercent}%</span><strong>{money(booking.feeCents)}</strong></li></ul><p className="muted">A porcentagem sustenta pagamentos, suporte, verificação, moderação e evolução da plataforma.</p></div><aside className="booking-box"><span className="eyebrow">Forma de pagamento</span><h2>Pix ou cartão</h2><p>A interface e a divisão já estão preparadas. A cobrança permanece em modo de testes até a conta Stripe Connect da Insidely ser vinculada.</p>{booking.status === "PENDING_PAYMENT" ? <form className="form-stack" action={payBookingAction.bind(null,booking.id)}><label className="payment-option"><input name="paymentMethod" type="radio" value="pix" defaultChecked/><span><strong>Pix</strong><small>Confirmação rápida</small></span></label><label className="payment-option"><input name="paymentMethod" type="radio" value="card"/><span><strong>Cartão</strong><small>Crédito ou débito compatível</small></span></label><button className="button button-accent button-block" type="submit">Confirmar pagamento de teste</button></form> : <p className="status">Consulta {booking.status.toLowerCase()}</p>}</aside></div></section></PublicShell>}
