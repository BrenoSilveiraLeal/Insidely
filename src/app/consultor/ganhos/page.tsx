import { PaymentStatus,Role } from "@prisma/client";import { DashboardShell } from "@/components/dashboard-shell";import { money,shortDate } from "@/lib/format";import { getConsultantDashboard } from "@/lib/queries";import { requireUser } from "@/lib/session";
export const dynamic="force-dynamic";export default async function Page(){const u=await requireUser([Role.CONSULTANT]);const p=await getConsultantDashboard(u.id);const paid=p?.bookings.filter(b=>b.payment?.status===PaymentStatus.APPROVED)??[];const total=paid.reduce((s,b)=>s+(b.payment?.amountCents??0),0);return <DashboardShell mode="consultant" title="Ganhos"><div className="metric" style={{marginBottom:24}}><span>Total bruto demonstrativo</span><strong>{money(total)}</strong><p className="muted">Nenhum repasse financeiro real ocorre neste MVP.</p></div><div className="table-wrap"><table><thead><tr><th>Data</th><th>Cliente</th><th>Valor</th><th>Pagamento</th></tr></thead><tbody>{paid.map(b=><tr key={b.id}><td>{shortDate(b.startsAt)}</td><td>{b.customer.name}</td><td>{money(b.payment?.amountCents??0)}</td><td><span className="status">APROVADO</span></td></tr>)}</tbody></table></div></DashboardShell>}

