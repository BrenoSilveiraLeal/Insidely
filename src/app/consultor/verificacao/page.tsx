import { Role } from "@prisma/client";import { submitVerificationAction } from "@/app/actions";import { DashboardShell } from "@/components/dashboard-shell";import { getConsultantDashboard } from "@/lib/queries";import { requireUser } from "@/lib/session";
export const dynamic="force-dynamic";export default async function Page(){const u=await requireUser([Role.CONSULTANT]);const p=await getConsultantDashboard(u.id);return <DashboardShell mode="consultant" title="Verificação"><div className="panel"><span className="eyebrow">Estado atual</span><h2>{p?.verificationStatus}</h2><p>A verificação confirma experiência declarada sem publicar documento ou dado sensível.</p><form action={submitVerificationAction}><button className="button button-dark" type="submit">Enviar nova solicitação simulada</button></form></div></DashboardShell>}

