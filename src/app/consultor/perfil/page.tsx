import { getPublicCompanies, getPublicProfessions } from "@/lib/queries";
import { Role, VerificationStatus } from "@/lib/domain";
import { BadgeCheck, Building2, CircleAlert, Clock3 } from "lucide-react";
import { DashboardShell } from "@/components/dashboard-shell";
import { AccountDangerZone, ProfessionalProfileForm, ProfileImageForm, VerificationRequestForm } from "@/components/profile-management";
import { getConsultantDashboard } from "@/lib/queries";
import { requireUser } from "@/lib/session";


export const dynamic = "force-dynamic";
const statusContent = { NOT_SUBMITTED: { label: "Ainda não verificado", copy: "Solicite a análise para mostrar que sua experiência profissional foi validada.", icon: CircleAlert }, PENDING: { label: "Em análise", copy: "Sua solicitação chegou à equipe. Você receberá uma atualização quando a análise terminar.", icon: Clock3 }, VERIFIED: { label: "Experiência verificada", copy: "Seu perfil já exibe o selo de experiência verificada.", icon: BadgeCheck }, REJECTED: { label: "Solicitação não aprovada", copy: "Você pode enviar uma nova solicitação escolhendo outra forma de comprovação.", icon: CircleAlert }, MORE_INFO_REQUIRED: { label: "Precisamos de mais informações", copy: "Envie uma nova solicitação com outra forma de comprovação.", icon: CircleAlert } } as const;
export default async function Page() { const user = await requireUser([Role.CONSULTANT]); const [profile, companies, professions] = await Promise.all([getConsultantDashboard(user.id), getPublicCompanies(), getPublicProfessions()]); const status = profile?.verificationStatus ?? VerificationStatus.NOT_SUBMITTED; const content = statusContent[status as keyof typeof statusContent] ?? statusContent.NOT_SUBMITTED; const StatusIcon = content.icon; const canRequest = status !== VerificationStatus.PENDING && status !== VerificationStatus.VERIFIED;
return <DashboardShell mode="consultant" title="Perfil e verifica" profileId={profile?.id}><div className="profile-management-stack"><ProfileImageForm image={profile?.user.image} name={profile?.user.name||user.name||"Pessoa"}/>{profile && <ProfessionalProfileForm profile={profile} companies={companies} professions={professions}/>}<div className="profile-admin-grid"><div className="profile-left-column"><AccountDangerZone email={user.email||""}/></div><section className={`panel verification-panel verification-${status.toLowerCase()}`} id="verificacao"><div className="verification-heading"><span className="verification-icon"><StatusIcon size={25}/></span><div><span className="eyebrow">Verifica profissional</span><h2>{content.label}</h2></div></div><p>{content.copy}</p>{canRequest && <VerificationRequestForm/>}{status === VerificationStatus.PENDING && <div className="verification-progress"><span/><span/><span/><small>Solicita recebida</small><small>An da experi</small><small>Resultado no perfil</small></div>}{status === VerificationStatus.VERIFIED && <div className="verified-company"><Building2 size={20}/><span>O selo confirma a experi analisada sem revelar dados privados.</span></div>}</section></div></div></DashboardShell>; }
