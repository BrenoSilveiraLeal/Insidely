import { completeOnboardingAction } from "@/app/actions";
import { requireUser } from "@/lib/session";
export default async function OnboardingPage(){const user=await requireUser();return <main className="auth-page"><section className="auth-art" style={{background:"var(--amber)"}}><h1>Seu contexto começa aqui.</h1></section><section className="auth-panel"><form action={completeOnboardingAction} className="auth-form form-stack"><span className="eyebrow">Olá, {user.name?.split(" ")[0]}</span><h2>Como você quer participar?</h2><div className="field"><label htmlFor="role">Perfil</label><select className="select" id="role" name="role" defaultValue={user.role === "CONSULTANT" ? "CONSULTANT" : "USER"}><option value="USER">Quero conversar com profissionais</option><option value="CONSULTANT">Quero compartilhar experiência</option></select></div><div className="field"><label htmlFor="headline">Título profissional (opcional)</label><input className="input" id="headline" name="headline" placeholder="Ex.: Product Designer em fintech"/></div><div className="field"><label htmlFor="location">Localização</label><input className="input" id="location" name="location" placeholder="Cidade, UF"/></div><div className="field"><label htmlFor="bio">O que você pode contextualizar?</label><textarea className="textarea" id="bio" name="bio"/></div><button className="button button-accent" type="submit">Concluir onboarding</button></form></section></main>}
