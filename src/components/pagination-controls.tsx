import Link from "next/link";
export function PaginationControls({page,total,pageSize,basePath}:{page:number;total:number;pageSize:number;basePath:string}){const pages=Math.max(1,Math.ceil(total/pageSize));if(pages<=1)return null;return <nav className="section-head" aria-label="Paginação"><span className="muted">Página {page} de {pages}</span><div style={{display:"flex",gap:8}}>{page>1&&<Link className="button button-ghost button-sm" href={`${basePath}?page=${page-1}`}>Anterior</Link>}{page<pages&&<Link className="button button-ghost button-sm" href={`${basePath}?page=${page+1}`}>Próxima</Link>}</div></nav>}
